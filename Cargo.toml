[package]
name = "powgrid-raix-cpu-miner"
version = "1.1.0"
edition = "2021"
authors = ["PowGrid Community Developers"]
description = "Multi-core AVX2 and SHA-NI accelerated RandomX CPU miner for Reticulum AI ($RAIX)"
license = "MIT OR Apache-2.0"
keywords = ["raix", "reticulum", "randomx", "cpu-miner"]

[dependencies]
sha2 = "0.10"
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "2.9", features = ["json"] }
libc = "0.2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
